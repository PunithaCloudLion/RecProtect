void Migration.effectiveDate()
{
	// 	c = 0;
	// 	getBoatRec = TrailerQuote[Effective_Date != null && Inception_Date == null];
	// 	info getBoatRec.count();
	// 	for each  getBoat in getBoatRec
	// 	{
	// 		if(getBoat.Effective_Date != null)
	// 		{
	// 			c = c + 1;
	// 			info "BoatQuote ID------------->" + getBoat.ID;
	// 			getBoat.Inception_Date=getBoat.Effective_Date;
	// 		}
	// 	}
	// 	info c;
}